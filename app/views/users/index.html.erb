<div class="container">


		<ul class="list-group">
			<% @users.each do |user| %>
			<li class="list-group-item"><%= user[:name] %></br><small><small>  joined <%= time_ago_in_words(user.created_at) %> ago </small></small></li>
			<% end %>
		</ul>


</div>